{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    ".env.*"
  ],
  "pipeline": {
    "build": {
      "outputs": [
        "apps/server/build/**",
        "common/constants/build/**",
        "common/schemas/build/**"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "db:types": {
      "outputs": [
        "common/schemas/src/database.ts"
      ],
      "dependsOn": [
        "^db:types"
      ]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
